---
import Link from '@components/Link.astro';

interface Props {
    url: string;
    text: string;
}

const { url, text } = Astro.props;
---

<Link url={url} text={text} className="inline-block w-48 rounded-full bg-green px-4 py-2 text-sm uppercase text-white hover:opacity-80 sm:text-base" />
