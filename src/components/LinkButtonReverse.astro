---
import Link from '@components/Link.astro';

interface Props {
    url: string;
    text: string;
}

const { url, text } = Astro.props;
---

<Link url={url} text={text} className="inline-block w-48 rounded-sm bg-white px-4 py-2 text-sm uppercase text-black hover:opacity-50 sm:text-base" />
